apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
 disableNameSuffixHash: true

commonLabels:
  application: amq-broker


bases:
- ../../../base/broker/deploy

resources:
  - mesh-broker.yaml
  # - edge-broker.yaml
  #- address.yaml
